# AGENTS.md

## 프로젝트 개요

이 프로젝트는 React 기반의 PC 웹 프로젝트다.

현재 단계에서는 **프론트엔드 퍼블리싱을 우선**하며, API 연동은 이후 진행한다.

---

## 1. 화면 지원 범위

* PC 웹 환경을 기준으로 구현한다.
* 모바일 및 태블릿 전용 반응형 디자인은 별도로 구현하지 않는다.
* 데스크톱 화면에서 디자인이 의도한 형태로 정확하게 표시되는 것을 우선한다.

---

## 2. 기존 컴포넌트 우선 사용

새로운 페이지나 기능을 구현하기 전에 반드시 기존 `components/`와 관련 페이지를 확인한다.

* 기존 컴포넌트로 구현할 수 있다면 반드시 기존 컴포넌트를 우선 재사용한다.
* 동일하거나 거의 동일한 컴포넌트를 새로 만들지 않는다.
* 기존 컴포넌트를 사용하는 것이 원칙이며, 불필요하게 새로운 버튼, 카드, 입력창 등을 만들지 않는다.
* 현재 프로젝트에 존재하는 공통 컴포넌트의 디자인과 동작을 최대한 유지한다.

예:

```text
components/
├── Button.jsx
├── Cardbox.jsx
├── Header.jsx
├── Input.jsx
├── schedule.jsx
├── small-green-button.jsx
├── small-red-button.jsx
└── small-white-button.jsx
```

버튼이나 카드 등이 필요할 경우 위와 같은 기존 컴포넌트를 먼저 확인한다.

---

## 3. 새 컴포넌트 생성 기준

기존 컴포넌트로 해결할 수 없는 경우에만 새로운 컴포넌트를 만든다.

* 여러 페이지에서 재사용할 가능성이 있는 컴포넌트 → `components/`
* 특정 페이지에서만 사용하는 컴포넌트 → 해당 페이지 폴더 내부
* 단순히 코드가 조금 다르다는 이유로 기존 공통 컴포넌트를 복제하지 않는다.
* 새 컴포넌트를 만들기 전에 기존 컴포넌트의 재사용 가능성을 먼저 검토한다.

---

## 4. 스타일 작성

페이지 전용 스타일은 해당 페이지 컴포넌트 파일에서 `styled-components`를 사용하여 작성한다.

예:

```jsx
const MyPage = () => {
  return (
    <Main>
      ...
    </Main>
  );
};

const Main = styled.main`
  ...
`;

export default MyPage;
```

* 페이지마다 별도의 스타일 파일을 무조건 생성하지 않는다.
* 여러 곳에서 반복적으로 사용되는 UI는 공통 컴포넌트로 분리한다.
* 기존 공통 컴포넌트가 있다면 직접 스타일을 새로 작성하지 않고 기존 컴포넌트를 사용한다.

---

## 5. 디자인 토큰

**기존 디자인 토큰을 최우선으로 사용한다.**

색상, 폰트, 간격 등 디자인 시스템에 정의된 값이 있다면 해당 토큰을 사용한다.

다음과 같은 디자인 값을 가능한 한 직접 하드코딩하지 않는다.

* 색상
* 폰트 크기
* 폰트 굵기
* 간격
* `border-radius`
* `box-shadow`
* 기타 디자인 시스템에 정의된 값

기존 토큰으로 표현할 수 있는 값을 임의의 새로운 값을 만들어 사용하지 않는다.

---

## 6. 기존 코드 수정

기존 코드를 수정할 때는 **필요한 부분만 최소한으로 수정한다.**

* 기존 구조와 동작을 최대한 유지한다.
* 현재 작업과 직접적인 관련이 없는 코드는 수정하지 않는다.
* 단순히 더 깔끔해 보인다는 이유로 기존 코드를 대규모로 리팩터링하지 않는다.
* 기존 컴포넌트와 스타일을 최대한 활용한다.

---

## 7. API 연결 전 퍼블리싱

현재 API가 연결되지 않은 경우 화면 구현을 위해 **mock/임시 데이터를 사용한다.**

예:

```js
const users = [
  {
    name: "홍길동",
    role: "USER",
  },
  {
    name: "김철수",
    role: "ADMIN",
  },
];
```

* 실제 API가 연결되지 않은 상태에서도 화면을 완성할 수 있도록 한다.
* 실제 API 연결 시 mock 데이터를 쉽게 교체할 수 있는 구조를 유지한다.
* 현재 퍼블리싱 단계에서는 API 자체를 새로 구현하지 않는다.

---

## 8. 로딩 / 빈 상태 / 에러 상태

현재는 퍼블리싱 단계이므로 **정상적으로 데이터가 존재하는 상태를 우선 구현한다.**

다음 상태는 현재 작업의 필수 범위로 포함하지 않는다.

* 로딩 상태
* 데이터가 없는 빈 상태
* API 에러 상태

API 연결 및 기능 구현 단계에서 필요에 따라 추가한다.

---

## 9. 접근성

기본적인 웹 접근성을 준수한다.

* 의미에 맞는 시맨틱 HTML을 우선 사용한다.
* 버튼은 가능한 한 `<button>`을 사용한다.
* 링크는 `<a>` 또는 프로젝트에서 사용하는 라우팅 방식에 맞는 요소를 사용한다.
* 이미지에는 적절한 `alt`를 제공한다.
* 입력 요소에는 적절한 `label`을 연결한다.
* 클릭 가능한 요소를 불필요하게 `<div>`로 구현하지 않는다.

---

## 10. 구현 우선순위

새로운 페이지를 구현할 때 다음 우선순위를 따른다.

1. 기존 페이지와 프로젝트 구조 확인
2. 기존 공통 컴포넌트 확인
3. 기존 디자인 토큰 확인
4. 기존 컴포넌트와 토큰을 최대한 재사용
5. 필요한 경우에만 새로운 컴포넌트 생성
6. 페이지 전용 스타일은 페이지 파일 내부에 작성
7. API가 없다면 mock 데이터로 화면 구현
8. 디자인 시안에 맞게 PC 웹 화면 구현

---

## 핵심 원칙

> **기존 것을 먼저 확인하고, 재사용할 수 있으면 재사용한다.**

> **기존 디자인 토큰과 컴포넌트를 우선 사용하며, 불필요한 새로운 코드와 중복을 만들지 않는다.**

> **현재는 PC 웹 퍼블리싱을 우선하고, API 연동 및 예외 상태는 이후 단계에서 처리한다.**
